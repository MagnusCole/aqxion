# AQXION - Codebase Completo Optimizado para LLMs

Este archivo contiene el codebase completo del proyecto AQXION organizado de manera jerárquica para facilitar el análisis por LLMs.

## 📋 Resumen del Proyecto

AQXION es una plataforma de adquisiciones estratégicas construida con Next.js 15, TypeScript, Tailwind CSS y desplegada en Vercel.

**Stack tecnológico:**
- Next.js 15 (App Router)
- TypeScript
- Tailwind CSS v3
- Vercel (deployment)
- PostgreSQL con Prisma (base de datos)
- Playwright (testing)

## 📂 Estructura del Proyecto

```
src/
├── app/                    # Next.js 15 App Router
├── components/             # Componentes UI (primitives, composables)
├── sections/              # Secciones de página principales
├── styles/                # Tokens de diseño, CSS base, utilidades
├── lib/                   # Utilidades y configuraciones
└── hooks/                 # React hooks personalizados
```

---

## 🔧 Archivos de Configuración

### package.json
```json
{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "lint:fix": "next lint --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf .next out node_modules/.cache",
    "analyze": "ANALYZE=true npm run build",
    "test:build": "npm run build && npm run start"
  },
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "gray-matter": "^4.0.3",
    "next": "^15.3.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "remark": "^15.0.1",
    "remark-gfm": "^4.0.1",
    "remark-html": "^16.0.1"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "@types/react": "^19.1.4",
    "@typescript-eslint/eslint-plugin": "^8.32.1",
    "@typescript-eslint/parser": "^8.32.1",
    "autoprefixer": "^10.4.21",
    "eslint": "^9",
    "eslint-config-next": "15.3.2",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
```

### tsconfig.json
```json
{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "es6"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### tailwind.config.js
```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/sections/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-geist-sans)', 'system-ui', 'sans-serif'],
        mono: ['var(--font-geist-mono)', 'monospace'],
      },
      colors: {
        background: 'var(--background)',
        foreground: 'var(--foreground)',
      },
      container: {
        center: true,
        padding: '1rem',
        screens: {
          sm: '640px',
          md: '768px',
          lg: '1024px',
          xl: '1280px',
        },
      },
    },
  },
  plugins: [],
}
```

### next.config.ts
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    optimizePackageImports: ['lucide-react']
  },
  images: {
    domains: ['localhost'],
    unoptimized: false,
  },
};

export default nextConfig;
```

---

## 🎨 Sistema de Estilos

### src/styles/globals.css
```css
@import './tokens.css';
@import './base.css';
@import './components.css';
@import './utilities.css';
@import './section-layout.css';
@import './responsive-utils.css';
@import './animations.css';

@import 'tailwindcss/base';
@import 'tailwindcss/components';
@import 'tailwindcss/utilities';

/* Critical styles for performance */
html {
  scroll-behavior: smooth;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  font-family: var(--font-sans, ui-sans-serif, system-ui, sans-serif);
  background-color: var(--color-bg-primary);
  color: var(--color-text-primary);
  margin: 0;
  padding: 0;
  line-height: 1.6;
}

* {
  box-sizing: border-box;
}

/* Accessibility improvements */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* Focus management */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.focus\:not-sr-only:focus {
  position: static;
  width: auto;
  height: auto;
  padding: 0;
  margin: 0;
  overflow: visible;
  clip: auto;
  white-space: normal;
}

/* Print styles */
@media print {
  .no-print {
    display: none !important;
  }
}
```

### src/styles/tokens.css
```css
/* Tokens de Diseño - AQXION */

:root {
  /* === Colores === */
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F8F9FB;
  --color-bg-elevated: #FFFFFF;
  --color-text-primary: #1A1F36;
  --color-text-secondary: #3C4257;
  --color-accent-main: #007AFF;
  --color-accent-cta: #FF6B35;
  --color-border: #E3E8EF;
  --color-success: #34D399;
  --color-warning: #FBBF24;
  --color-error: #EF4444;

  /* Gradientes */
  --gradient-primary: linear-gradient(135deg, #007AFF 0%, #5856D6 100%);
  --gradient-secondary: linear-gradient(135deg, #FF6B35 0%, #F093FB 100%);
  
  /* === Tipografía === */
  --font-sans: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  --font-mono: ui-monospace, SFMono-Regular, "SF Mono", Monaco, Consolas, monospace;
  
  /* Escala tipográfica */
  --text-xs: 0.75rem;    /* 12px */
  --text-sm: 0.875rem;   /* 14px */
  --text-base: 1rem;     /* 16px */
  --text-lg: 1.125rem;   /* 18px */
  --text-xl: 1.25rem;    /* 20px */
  --text-2xl: 1.5rem;    /* 24px */
  --text-3xl: 1.875rem;  /* 30px */
  --text-4xl: 2.25rem;   /* 36px */
  --text-5xl: 3rem;      /* 48px */
  --text-6xl: 3.75rem;   /* 60px */
  
  /* === Espaciado === */
  --spacing-1: 0.25rem;  /* 4px */
  --spacing-2: 0.5rem;   /* 8px */
  --spacing-3: 0.75rem;  /* 12px */
  --spacing-4: 1rem;     /* 16px */
  --spacing-5: 1.25rem;  /* 20px */
  --spacing-6: 1.5rem;   /* 24px */
  --spacing-8: 2rem;     /* 32px */
  --spacing-10: 2.5rem;  /* 40px */
  --spacing-12: 3rem;    /* 48px */
  --spacing-16: 4rem;    /* 64px */
  --spacing-20: 5rem;    /* 80px */
  --spacing-24: 6rem;    /* 96px */
  
  /* Espaciado de secciones */
  --spacing-section-y: 5rem;    /* 80px */
  --spacing-section-sm-y: 3rem; /* 48px */
  
  /* === Contenedores === */
  --container-sm: 640px;
  --container-md: 768px;
  --container-lg: 1024px;
  --container-xl: 1280px;
  --container-2xl: 1536px;
  
  /* === Shadows === */
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-base: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
  --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
  --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
  
  /* === Bordes === */
  --radius-sm: 0.25rem;  /* 4px */
  --radius-base: 0.375rem; /* 6px */
  --radius-md: 0.5rem;   /* 8px */
  --radius-lg: 0.75rem;  /* 12px */
  --radius-xl: 1rem;     /* 16px */
  --radius-2xl: 1.5rem;  /* 24px */
  --radius-full: 9999px;
  
  /* === Transiciones === */
  --transition-fast: 150ms ease-in-out;
  --transition-base: 250ms ease-in-out;
  --transition-slow: 350ms ease-in-out;
  
  /* === Z-index === */
  --z-dropdown: 1000;
  --z-sticky: 1020;
  --z-fixed: 1030;
  --z-modal-backdrop: 1040;
  --z-modal: 1050;
  --z-popover: 1060;
  --z-tooltip: 1070;
  --z-toast: 1080;
}

/* Dark mode tokens (para futuro uso) */
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-primary: #0F1419;
    --color-bg-secondary: #1A1F2E;
    --color-bg-elevated: #252A3A;
    --color-text-primary: #F8F9FB;
    --color-text-secondary: #B4BCD0;
    --color-border: #2D3446;
  }
}
```

---

## 🏗️ Arquitectura de Componentes

### src/app/layout.tsx
```tsx
import type { Metadata } from "next";
import localFont from "next/font/local";
import "../styles/globals.css";
import { GoogleTagManager, GoogleTagManagerNoScript } from "@/components/analytics/GoogleTagManager";
import { AnalyticsWrapper } from "@/components/analytics/AnalyticsWrapper";
import { Header } from "@/components/composables/navigation/Header";
import { FooterSectionSimple } from "@/sections/FooterSectionSimple";
import { CookieBanner } from "@/components/composables/marketing/CookieBanner";
import { Logo } from "@/components/atoms/Logo";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff2",
  variable: "--font-geist-sans",
  weight: "100 900",
});

const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff2",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "AQXION - Adquisiciones Estratégicas",
  description: "Plataforma líder en adquisiciones estratégicas para el crecimiento empresarial sostenible.",
  metadataBase: new URL('https://aqxion.com'),
  openGraph: {
    title: "AQXION - Adquisiciones Estratégicas",
    description: "Plataforma líder en adquisiciones estratégicas para el crecimiento empresarial sostenible.",
    url: 'https://aqxion.com',
    siteName: 'AQXION',
    images: [
      {
        url: '/logo.ico',
        width: 1200,
        height: 630,
        alt: 'AQXION Logo',
      },
    ],
    locale: 'es_ES',
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
    title: "AQXION - Adquisiciones Estratégicas",
    description: "Plataforma líder en adquisiciones estratégicas para el crecimiento empresarial sostenible.",
    images: ['/logo.ico'],
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      'max-video-preview': -1,
      'max-image-preview': 'large',
      'max-snippet': -1,
    },
  },
};

const footerLinks = [
  { href: "/terminos-privacidad", label: "Términos y Privacidad" },
  { href: "/contacto", label: "Contacto" },
];

export default function RootLayout({ children }: { children: React.ReactNode }) {
  const gtmId = process.env.NEXT_PUBLIC_GTM_ID || '';
  
  return (
    <html
      lang="es"
      className={`${geistSans.variable} ${geistMono.variable} scroll-smooth`}
      data-theme="light"
    >
      <head>
        <GoogleTagManager gtmId={gtmId || 'GTM-TGD5LDTN'} />
        <style dangerouslySetInnerHTML={{
          __html: `
            :root {
              --color-bg-primary: #FFFFFF;
              --color-bg-secondary: #F8F9FB;
              --color-text-primary: #1A1F36;
              --color-text-secondary: #3C4257;
              --color-accent-main: #007AFF;
              --color-accent-cta: #FF6B35;
              --color-border: #E3E8EF;
            }
            body {
              font-family: var(--font-sans, ui-sans-serif, system-ui, sans-serif);
              background-color: var(--color-bg-primary);
              color: var(--color-text-primary);
              margin: 0;
              padding: 0;
              line-height: 1.6;
            }
            * {
              box-sizing: border-box;
            }
          `
        }} />
      </head>
      <body className="bg-[var(--color-bg-primary)] text-[var(--color-text-primary)] antialiased font-sans min-h-screen flex flex-col">
        <GoogleTagManagerNoScript gtmId={gtmId || 'GTM-TGD5LDTN'} />
        <AnalyticsWrapper>
          <a
            href="#main"
            className="sr-only focus:not-sr-only focus:absolute focus:top-0 focus:left-0 focus:p-4 focus:z-50 focus:bg-black focus:text-white"
          >
            Saltar al contenido principal
          </a>
          <Header logo={<Logo />} />
          <main id="main" className="flex-grow">
            {children}
          </main>
          <FooterSectionSimple 
            links={footerLinks}
            copyright={`© ${new Date().getFullYear()} AQXION. Todos los derechos reservados.`}
          />
          <CookieBanner />
        </AnalyticsWrapper>
      </body>
    </html>
  );
}
```

### src/app/page.tsx
```tsx
// AI OPTIMIZATION: Imports consolidados y estructura simplificada
import { HeroSection } from "@/sections/HeroSection";
import { ProblemSection } from "@/sections/ProblemSection";
import { PainPointSection } from "@/sections/PainPointSection";
import { SolutionSection } from "@/sections/SolutionSection";
import { CTASection } from "@/sections/CTASection";

// AI OPTIMIZATION: Homepage optimizada para conversión máxima con flujo PAS
export default function Home() {
  return (
    <main className="min-h-screen">
      <HeroSection />
      <ProblemSection />
      <PainPointSection />
      <SolutionSection />
      <CTASection />
    </main>
  );
}
```

---

## 🧩 Componentes Principales

### src/components/atoms/Button.tsx
```tsx
import React from 'react';
import { VariantProps, cva } from 'class-variance-authority';

const buttonVariants = cva(
  // Base classes
  'inline-flex items-center justify-center rounded-lg font-medium transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 disabled:opacity-50 disabled:pointer-events-none',
  {
    variants: {
      variant: {
        primary: 'bg-[var(--color-accent-main)] text-white hover:bg-[#0056CC] focus:ring-[var(--color-accent-main)]',
        secondary: 'bg-[var(--color-accent-cta)] text-white hover:bg-[#E55A2B] focus:ring-[var(--color-accent-cta)]',
        outline: 'border-2 border-[var(--color-accent-main)] text-[var(--color-accent-main)] hover:bg-[var(--color-accent-main)] hover:text-white',
        ghost: 'text-[var(--color-text-primary)] hover:bg-[var(--color-bg-secondary)]',
        link: 'text-[var(--color-accent-main)] underline-offset-4 hover:underline',
      },
      size: {
        sm: 'h-9 px-3 text-sm',
        md: 'h-10 px-4 py-2',
        lg: 'h-11 px-8',
        xl: 'h-12 px-10 text-lg',
      },
      width: {
        auto: 'w-auto',
        full: 'w-full',
      },
    },
    defaultVariants: {
      variant: 'primary',
      size: 'md',
      width: 'auto',
    },
  }
);

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, width, asChild = false, ...props }, ref) => {
    return (
      <button
        className={`${buttonVariants({ variant, size, width })} ${className || ''}`}
        ref={ref}
        {...props}
      />
    );
  }
);

Button.displayName = 'Button';

export { Button, buttonVariants };
```

### src/components/atoms/Heading.tsx
```tsx
import React from 'react';
import { VariantProps, cva } from 'class-variance-authority';

const headingVariants = cva(
  'font-bold leading-tight tracking-tight',
  {
    variants: {
      level: {
        1: 'text-4xl md:text-5xl lg:text-6xl',
        2: 'text-3xl md:text-4xl lg:text-5xl',
        3: 'text-2xl md:text-3xl lg:text-4xl',
        4: 'text-xl md:text-2xl lg:text-3xl',
        5: 'text-lg md:text-xl lg:text-2xl',
        6: 'text-base md:text-lg lg:text-xl',
      },
      color: {
        primary: 'text-[var(--color-text-primary)]',
        secondary: 'text-[var(--color-text-secondary)]',
        accent: 'text-[var(--color-accent-main)]',
        white: 'text-white',
      },
      align: {
        left: 'text-left',
        center: 'text-center',
        right: 'text-right',
      },
    },
    defaultVariants: {
      level: 2,
      color: 'primary',
      align: 'left',
    },
  }
);

export interface HeadingProps
  extends React.HTMLAttributes<HTMLHeadingElement>,
    VariantProps<typeof headingVariants> {
  as?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
}

const Heading = React.forwardRef<HTMLHeadingElement, HeadingProps>(
  ({ className, level, color, align, as, children, ...props }, ref) => {
    const Component = as || `h${level || 2}` as keyof JSX.IntrinsicElements;

    return (
      <Component
        className={`${headingVariants({ level, color, align })} ${className || ''}`}
        ref={ref as any}
        {...props}
      >
        {children}
      </Component>
    );
  }
);

Heading.displayName = 'Heading';

export { Heading, headingVariants };
```

### src/components/composables/layout/Section.tsx
```tsx
import React from 'react';
import { VariantProps, cva } from 'class-variance-authority';

const sectionVariants = cva(
  'w-full relative',
  {
    variants: {
      variant: {
        default: 'bg-[var(--color-bg-primary)]',
        secondary: 'bg-[var(--color-bg-secondary)]',
        dark: 'bg-[var(--color-text-primary)] text-white',
        gradient: 'bg-gradient-to-br from-[var(--color-accent-main)] to-[var(--color-accent-cta)] text-white',
      },
      padding: {
        none: 'py-0',
        sm: 'py-8 md:py-12',
        md: 'py-12 md:py-16',
        lg: 'py-16 md:py-20',
        xl: 'py-20 md:py-24',
        '2xl': 'py-24 md:py-32',
      },
      container: {
        true: '',
        false: '',
      },
    },
    defaultVariants: {
      variant: 'default',
      padding: 'lg',
      container: true,
    },
  }
);

export interface SectionProps
  extends React.HTMLAttributes<HTMLElement>,
    VariantProps<typeof sectionVariants> {
  as?: 'section' | 'div' | 'article' | 'aside';
}

const Section = React.forwardRef<HTMLElement, SectionProps>(
  ({ className, variant, padding, container, as = 'section', children, ...props }, ref) => {
    const Component = as;

    return (
      <Component
        className={`${sectionVariants({ variant, padding })} ${className || ''}`}
        ref={ref as any}
        {...props}
      >
        {container ? (
          <div className="container mx-auto px-4 md:px-6 lg:px-8">
            {children}
          </div>
        ) : (
          children
        )}
      </Component>
    );
  }
);

Section.displayName = 'Section';

export { Section, sectionVariants };
```

---

## 📄 Secciones Principales

### src/sections/HeroSection.tsx
```tsx
'use client';

import React from 'react';
import { Section } from '@/components/composables/layout/Section';
import { Heading } from '@/components/atoms/Heading';
import { Text } from '@/components/atoms/Text';
import { Button } from '@/components/atoms/Button';
import { Container } from '@/components/composables/layout/Container';

export function HeroSection() {
  return (
    <Section variant="gradient" padding="2xl" className="min-h-screen flex items-center">
      <Container>
        <div className="text-center space-y-8">
          <div className="space-y-4">
            <Heading as="h1" level={1} color="white" align="center">
              Adquisiciones Estratégicas que{' '}
              <span className="text-[var(--color-accent-cta)]">Transforman</span>{' '}
              Empresas
            </Heading>
            <Text size="xl" color="white" align="center" className="max-w-3xl mx-auto">
              Conectamos empresarios visionarios con oportunidades de adquisición excepcionales
              para crear valor sostenible a largo plazo.
            </Text>
          </div>
          
          <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <Button variant="secondary" size="xl">
              Explorar Oportunidades
            </Button>
            <Button variant="outline" size="xl" className="border-white text-white hover:bg-white hover:text-[var(--color-text-primary)]">
              Conocer Más
            </Button>
          </div>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8 mt-16">
            <div className="text-center">
              <div className="text-4xl font-bold text-white mb-2">$50M+</div>
              <div className="text-white/80">En transacciones exitosas</div>
            </div>
            <div className="text-center">
              <div className="text-4xl font-bold text-white mb-2">25+</div>
              <div className="text-white/80">Empresas adquiridas</div>
            </div>
            <div className="text-center">
              <div className="text-4xl font-bold text-white mb-2">15+</div>
              <div className="text-white/80">Años de experiencia</div>
            </div>
          </div>
        </div>
      </Container>
    </Section>
  );
}
```

### src/sections/ProblemSection.tsx
```tsx
'use client';

import React from 'react';
import { Section } from '@/components/composables/layout/Section';
import { Heading } from '@/components/atoms/Heading';
import { Text } from '@/components/atoms/Text';
import { Container } from '@/components/composables/layout/Container';

export function ProblemSection() {
  const problems = [
    {
      title: "Falta de Oportunidades Curadas",
      description: "Los empresarios pierden tiempo evaluando negocios que no se alinean con su visión estratégica."
    },
    {
      title: "Procesos de Due Diligence Complejos",
      description: "La evaluación exhaustiva de empresas requiere experiencia especializada que muchos no poseen."
    },
    {
      title: "Acceso Limitado a Dealflow Premium",
      description: "Las mejores oportunidades de adquisición circulan en círculos cerrados, fuera del alcance de muchos inversores."
    }
  ];

  return (
    <Section variant="secondary" padding="xl">
      <Container>
        <div className="text-center mb-16">
          <Heading as="h2" level={2} align="center" className="mb-6">
            El Desafío del Mercado de Adquisiciones
          </Heading>
          <Text size="lg" color="secondary" align="center" className="max-w-3xl mx-auto">
            El panorama actual presenta obstáculos significativos para empresarios que buscan
            crecer a través de adquisiciones estratégicas.
          </Text>
        </div>
        
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {problems.map((problem, index) => (
            <div key={index} className="bg-white p-8 rounded-lg shadow-sm border border-[var(--color-border)]">
              <div className="w-12 h-12 bg-red-100 rounded-lg flex items-center justify-center mb-6">
                <span className="text-2xl">⚠️</span>
              </div>
              <Heading as="h3" level={4} className="mb-4">
                {problem.title}
              </Heading>
              <Text color="secondary">
                {problem.description}
              </Text>
            </div>
          ))}
        </div>
      </Container>
    </Section>
  );
}
```

### src/sections/SolutionSection.tsx
```tsx
'use client';

import React from 'react';
import { Section } from '@/components/composables/layout/Section';
import { Heading } from '@/components/atoms/Heading';
import { Text } from '@/components/atoms/Text';
import { Container } from '@/components/composables/layout/Container';
import { Button } from '@/components/atoms/Button';

export function SolutionSection() {
  const solutions = [
    {
      title: "Dealflow Curado",
      description: "Acceso exclusivo a oportunidades pre-evaluadas que se alinean con criterios de inversión específicos.",
      icon: "🎯"
    },
    {
      title: "Due Diligence Profesional",
      description: "Equipo de expertos que realiza análisis exhaustivos financieros, operacionales y estratégicos.",
      icon: "🔍"
    },
    {
      title: "Acompañamiento Integral",
      description: "Soporte desde la identificación hasta la integración post-adquisición para maximizar el valor.",
      icon: "🤝"
    },
    {
      title: "Red de Contactos Premium",
      description: "Acceso a una red exclusiva de vendedores, inversores y asesores estratégicos.",
      icon: "🌐"
    }
  ];

  return (
    <Section variant="default" padding="xl">
      <Container>
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
          <div>
            <Heading as="h2" level={2} className="mb-6">
              La Plataforma que{' '}
              <span className="text-[var(--color-accent-main)]">Simplifica</span>{' '}
              las Adquisiciones
            </Heading>
            <Text size="lg" color="secondary" className="mb-8">
              AQXION combina tecnología avanzada con experiencia humana para crear
              un ecosistema donde las adquisiciones estratégicas se ejecutan con
              precisión y eficiencia.
            </Text>
            <Button variant="primary" size="lg">
              Solicitar Demo
            </Button>
          </div>
          
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-6">
            {solutions.map((solution, index) => (
              <div key={index} className="bg-[var(--color-bg-secondary)] p-6 rounded-lg">
                <div className="text-3xl mb-4">{solution.icon}</div>
                <Heading as="h3" level={5} className="mb-3">
                  {solution.title}
                </Heading>
                <Text size="sm" color="secondary">
                  {solution.description}
                </Text>
              </div>
            ))}
          </div>
        </div>
      </Container>
    </Section>
  );
}
```

---

## 🔧 Utilidades y Configuraciones

### src/lib/analytics.ts
```typescript
// Analytics configuration and utilities
declare global {
  interface Window {
    gtag: (command: string, targetId: string, config?: Record<string, any>) => void;
    dataLayer: any[];
  }
}

export const GA_TRACKING_ID = process.env.NEXT_PUBLIC_GA_ID || '';

// Track page views
export const pageview = (url: string) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('config', GA_TRACKING_ID, {
      page_location: url,
    });
  }
};

// Track events
export const event = (action: string, parameters?: Record<string, any>) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', action, parameters);
  }
};

// Track conversions
export const trackConversion = (conversionLabel: string, value?: number) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', 'conversion', {
      send_to: `${GA_TRACKING_ID}/${conversionLabel}`,
      value: value,
      currency: 'USD',
    });
  }
};

// Track form submissions
export const trackFormSubmission = (formName: string, formData?: Record<string, any>) => {
  event('form_submit', {
    form_name: formName,
    ...formData,
  });
};

// Track button clicks
export const trackButtonClick = (buttonName: string, location?: string) => {
  event('click', {
    button_name: buttonName,
    click_location: location,
  });
};
```

### src/lib/config.ts
```typescript
export const config = {
  // Site configuration
  site: {
    name: 'AQXION',
    description: 'Plataforma líder en adquisiciones estratégicas para el crecimiento empresarial sostenible.',
    url: process.env.NEXT_PUBLIC_SITE_URL || 'https://aqxion.com',
    logo: '/logo.ico',
  },
  
  // Contact information
  contact: {
    email: process.env.NEXT_PUBLIC_CONTACT_EMAIL || 'contacto@aqxion.com',
    phone: process.env.NEXT_PUBLIC_CONTACT_PHONE || '+1 (555) 123-4567',
  },
  
  // Social media links
  social: {
    linkedin: 'https://linkedin.com/company/aqxion',
    twitter: 'https://twitter.com/aqxion',
  },
  
  // Analytics
  analytics: {
    gtmId: process.env.NEXT_PUBLIC_GTM_ID || '',
    gaId: process.env.NEXT_PUBLIC_GA_ID || '',
  },
  
  // Features flags
  features: {
    blog: true,
    contactForm: true,
    newsletter: true,
    cookieBanner: true,
  },
} as const;
```

### src/hooks/useAnalytics.tsx
```tsx
'use client';

import { useEffect } from 'react';
import { usePathname, useSearchParams } from 'next/navigation';
import { pageview } from '@/lib/analytics';

export function useAnalytics() {
  const pathname = usePathname();
  const searchParams = useSearchParams();

  useEffect(() => {
    if (pathname) {
      const url = pathname + (searchParams?.toString() ? `?${searchParams.toString()}` : '');
      pageview(url);
    }
  }, [pathname, searchParams]);
}
```

---

## 📚 Documentación Adicional

### README.md
```markdown
# AQXION — Strategic Acquisition Platform

> **Vision:** Operator‑led, vision‑driven acquisitions across LATAM.  
> **Stack:** Next.js 15 · TypeScript · Tailwind CSS v3 · Vercel · PostgreSQL (Prisma) · Playwright.

## Quick Start

```bash
git clone https://github.com/AQXION/aqxion-app.git
cd aqxion-app
pnpm install
cp .env.example .env.local   # ⬅️ fill DB_URL & NEXT_PUBLIC_SITE_URL
pnpm dev
```

Site runs at `http://localhost:3000`.

## Core Scripts

| Command            | Purpose                          |
|--------------------|----------------------------------|
| `pnpm dev`         | Local dev (Next.js)              |
| `pnpm build`       | Production build                 |
| `pnpm lint`        | ESLint + Prettier                |
| `pnpm test`        | Vitest unit suite                |
| `pnpm test:e2e`    | Playwright end‑to‑end            |
| `pnpm prisma:mig`  | Run DB migrations                |

## Repo Map

```
src/
 ├ app/            # Next.js 15 App Router
 ├ components/     # UI primitives (layout, sections, ui)
 ├ styles/         # tokens, base, utilities, components
 └ tests/          # unit + e2e
docs/
 ├ DESIGN_SYSTEM.md
 └ ARCHITECTURE.md
```

## Status

| Env | URL | Branch |
|-----|-----|--------|
| **Production** | https://aqxion.com | `main` |
| **Preview** | Vercel PR previews | `feat/*` |

## License

© 2025 AQXION Holdings. All rights reserved.
```

---

## 🎯 Características Clave del Proyecto

### Performance
- **Optimización de Core Web Vitals**: CSS crítico inline, lazy loading de imágenes
- **Carga progresiva**: Componentes optimizados con React.memo y lazy loading
- **Minificación**: Assets optimizados automáticamente por Next.js 15

### SEO
- **Metadata optimizada**: Open Graph, Twitter Cards, structured data
- **Sitemap automático**: Generado dinámicamente desde el contenido
- **Schema markup**: Implementado para mejor indexación

### Accesibilidad
- **ARIA labels**: Implementados en todos los componentes interactivos
- **Navegación por teclado**: Soporte completo para usuarios de teclado
- **Contraste**: Cumple con WCAG 2.1 AA standards
- **Screen readers**: Texto alternativo y estructura semántica

### Analítica
- **Google Analytics 4**: Seguimiento avanzado de eventos y conversiones
- **Google Tag Manager**: Gestión centralizada de tags
- **Conversion tracking**: Seguimiento de formularios y CTAs

### Escalabilidad
- **Arquitectura modular**: Componentes reutilizables y mantenibles
- **Type safety**: TypeScript en toda la aplicación
- **Design system**: Tokens de diseño consistentes y documentados

---

**Nota**: Este codebase está optimizado para análisis por LLMs y contiene los componentes principales del proyecto AQXION. Para acceder al código completo y actualizaciones, consulte el repositorio oficial.
